<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- TextView untuk label OTP -->
    <TextView
        android:id="@+id/otpTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Masukkan OTP"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="?attr/actionBarSize"
        android:layout_marginBottom="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- LinearLayout untuk input OTP -->
    <LinearLayout
        android:id="@+id/otpContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/otpTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- EditText untuk digit pertama -->
        <EditText
            android:id="@+id/otpDigit1"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:inputType="text"
            android:maxLength="1"
            android:gravity="center"
            android:textSize="18sp"
            android:background="@drawable/otp_input_background"
            android:layout_marginEnd="8dp" />

        <!-- EditText untuk digit kedua -->
        <EditText
            android:id="@+id/otpDigit2"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:inputType="text"
            android:maxLength="1"
            android:gravity="center"
            android:textSize="18sp"
            android:background="@drawable/otp_input_background"
            android:layout_marginEnd="8dp" />

        <!-- EditText untuk digit ketiga -->
        <EditText
            android:id="@+id/otpDigit3"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:inputType="text"
            android:maxLength="1"
            android:gravity="center"
            android:textSize="18sp"
            android:background="@drawable/otp_input_background"
            android:layout_marginEnd="8dp" />

        <!-- EditText untuk digit keempat -->
        <EditText
            android:id="@+id/otpDigit4"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:inputType="text"
            android:maxLength="1"
            android:gravity="center"
            android:textSize="18sp"
            android:background="@drawable/otp_input_background"
            android:layout_marginEnd="8dp" />

        <!-- EditText untuk digit kelima -->
        <EditText
            android:id="@+id/otpDigit5"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:inputType="text"
            android:maxLength="1"
            android:gravity="center"
            android:textSize="18sp"
            android:background="@drawable/otp_input_background"
            android:layout_marginEnd="8dp" />

        <!-- EditText untuk digit keenam -->
        <EditText
            android:id="@+id/otpDigit6"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:inputType="text"
            android:maxLength="1"
            android:gravity="center"
            android:textSize="18sp"
            android:background="@drawable/otp_input_background" />
    </LinearLayout>

    <!-- Tombol untuk verifikasi -->
    <Button
        android:id="@+id/verifyOtpButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:text="Verifikasi OTP"
        android:background="@drawable/button_background"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/otpContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
